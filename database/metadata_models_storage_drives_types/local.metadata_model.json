{
    "$FIELD_GROUP_KEY": "$",
    "$FIELD_GROUP_NAME": "local",
    "$FIELD_GROUP_DESCRIPTION": "Storage in folder found at path. Ensure that path exists and is mounted.",
    "$FIELD_GROUP_MAX_ENTRIES": 1,
    "$GROUP_FIELDS": [
        {
            "path": {
                "$FIELD_GROUP_KEY": "$.$GROUP_FIELDS[*].path",
                "$FIELD_GROUP_NAME": "path",
                "$FIELD_GROUP_MAX_ENTRIES": 1,
                "$FIELD_DATATYPE": "text",
                "$FIELD_UI": "textarea",
                "$FIELD_GROUP_DESCRIPTION": "NB. Ensure that path exists and is mounted."
            }
        }
    ],
    "$GROUP_READ_ORDER_OF_FIELDS": [
        "path"
    ]
}